import { SearchcontentPipe } from './searchcontent.pipe';

describe('SearchcontentPipe', () => {
  it('create an instance', () => {
    const pipe = new SearchcontentPipe();
    expect(pipe).toBeTruthy();
  });
});
